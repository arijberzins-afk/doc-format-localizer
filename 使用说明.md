# 政府公文格式保留本地化工具 使用说明

将外省政府公文（PDF）的省份、机关等地域信息替换为福建省对应信息，**完全保留原始版面、字体、颜色和排版**，不重新生成文档。

---

## 环境要求

- Python 3.9+
- Windows 系统（依赖系统字体）
- 阿里云百炼 API Key（用于 LLM 生成替换词表）

---

## 安装依赖

```bash
pip install pymupdf pillow numpy requests
```

---

## 配置

将 `config.json.template` 复制一份，重命名为 `config.json`，填入 API Key：

```json
{
  "qwen_api_key": "你的API Key",
  "qwen_endpoint": "https://dashscope.aliyuncs.com/compatible-mode/v1",
  "llm_disable_proxy": false
}
```

> 如使用阿里云百炼工作空间专属 endpoint，将 `qwen_endpoint` 替换为工作空间地址，并将 `llm_disable_proxy` 设为 `true`。

---

## 使用方法

### 处理单个文件

```bash
python scripts/localize_preserve_format.py \
  --input C:/Users/你的用户名/Desktop/文件.pdf \
  --target-province "福建省" \
  --target-authority "福建省人力资源和社会保障厅" \
  --config config.json
```

### 批量处理多个文件

```bash
python scripts/localize_preserve_format.py \
  --input C:/Users/你的用户名/Desktop/文件1.pdf \
  --input C:/Users/你的用户名/Desktop/文件2.pdf \
  --input C:/Users/你的用户名/Desktop/文件3.pdf \
  --target-province "福建省" \
  --target-authority "福建省人力资源和社会保障厅" \
  --config config.json
```

### 跳过预览图（处理更快）

```bash
python scripts/localize_preserve_format.py \
  --input 文件.pdf \
  --target-province "福建省" \
  --target-authority "福建省人力资源和社会保障厅" \
  --config config.json \
  --no-preview
```

---

## 输出结果

- 输出位置：**桌面**
- 文件命名：`原文件名_localized.pdf`
- 同时输出每页 PNG 预览图（加 `--no-preview` 可跳过）

---

## 替换逻辑说明

工具通过 LLM 分析文件内容，自动识别需要替换的地名：

- **替换**：省份名、直辖市名（如 `广东省` → `福建省`，`重庆市` → `福建省`）
- **替换**：发文字号中的省市简称（如 `粤人社规〔2022〕22号` → `闽人社规〔2022〕22号`）
- **不替换**：正文叙述中的地名（如"与广东省签订协议"不会被改动）
- **不替换**：企业名称、项目名称中的地名

替换词长度保持一致，确保版面不变形。

---

## 常见问题

**Q：支持哪些文件格式？**  
A：目前主要支持 PDF（文字层）。图片格式（JPG/PNG）需额外安装 `rapidocr-onnxruntime` 且需要 Qwen-VL 图像理解 API。

**Q：需要方正小标宋字体吗？**  
A：如果原文件红头使用方正小标宋，建议将字体文件放入 `fonts/` 目录，否则自动回退到宋体。

**Q：处理速度慢怎么办？**  
A：加 `--no-preview` 跳过 PNG 生成；网络较慢时 LLM 调用耗时较长，属正常现象。

**Q：某些词没有被替换？**  
A：LLM 仅替换标准地名，不会替换正文中的历史性叙述地名，这是设计行为。

---

## 字体说明

脚本自动调用 Windows 系统字体，无需额外安装：

| 用途 | 字体 |
|------|------|
| 正文 | 仿宋（simfang.ttf） |
| 标题 | 黑体（simhei.ttf） |
| 红头 | 方正小标宋（需自备，放入 fonts/ 目录） |

